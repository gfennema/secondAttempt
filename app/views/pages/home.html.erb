<div class="home-callout text-right container-fluid">
  <h1>This is GraphicDesign.</h1>
  <h3>A site where you can have your graphic design needs realized!</h3>
</div>
<div class="container home-signup-link">
  <div class="row">
    <div class="col-md-6">
      <div class="well">
        <h3 class="text-center">BasicMembership</h3>
        <h4 class="text-center">Sign up for free and get basic access to the community.</h4>
        <br/>
        <%= link_to "Sign Up for a Basic Membership", new_user_registration_path(plan: @basic_plan.id), class: 'btn btn-danger btn-lg btn-block' %>
      </div>
    </div>
    <div class="col-md-6">
      <div class="well">
        <h3 class="text-center">ProMembership</h3>
        <h4 class="text-center">Sign up for $10/month and get access to the community and contact information!</h4>
        <br/>
        <%= link_to "Sign Up for a Pro Membership", new_user_registration_path(plan: @pro_plan.id), class: 'btn btn-primary btn-lg btn-block' %>
      </div>
    </div>
  </div>
  <%= link_to "Create Your Profile!", new_user_profile_path(user_id: current_user.id) if user_signed_in? %>
</div>
  